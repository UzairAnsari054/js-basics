"use strict"; // treat all JS code as newer version

// alert(3+3) alert is not defined because we are using nodejs, not browser

let name = "uzair"
let age = 25
let isLoggedIn = true

// null => stand alone value, In Js null type is object
// undefined => 
// symbol => unique

// object

console.log(typeof null);